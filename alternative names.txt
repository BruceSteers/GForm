many commands / arguments have alternative words can be used.
the following list is a list of commands and the alternatives you can use.

seperaror, sep
listseperator, lsep
toolbutton, fbutton
tbutton, togglebutton, tbutton
input, textbox
password, starbox, maskbox
gridbox, gridview
returnname, returnval , same as return=name
return, returnall same as return=all
allstretch, stretchall
topmost, toponly
version, ver
